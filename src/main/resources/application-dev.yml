spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost/blogtest2?characterEncoding=UTF-8
    username: root
    password: 0174014XZP
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: info
    com.peng.blog: debug
  file: log/blog-dev.log

peng:
  upload:
    accessPath: /uploadFile/**
    resourcesLocation: file:/D:/testUpload/

